<%= form_for(@log_datum) do |f| %>
  <% if @log_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@log_datum.errors.count, "error") %> prohibited this log_datum from being saved:</h2>

      <ul>
      <% @log_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session %><br>
    <%= f.text_field :session %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.datetime_select :time %>
  </div>
  <div class="field">
    <%= f.label :timeDrift %><br>
    <%= f.number_field :timeDrift %>
  </div>
  <div class="field">
    <%= f.label :event %><br>
    <%= f.text_field :event %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
